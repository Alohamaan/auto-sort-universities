services:
  app:
    image: ghcr.io/${OWNER}/admissions-agent:${APP_IMAGE_TAG:-latest}
    env_file:
      - .env
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
